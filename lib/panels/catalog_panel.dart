import 'package:flutter/material.dart';

class CatalogPanel extends StatelessWidget {
  const CatalogPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 2000, child: Placeholder());
  }
}
